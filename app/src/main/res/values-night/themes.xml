<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="dark_mode" parent="Theme.Material3.DayNight">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/purple</item>
        <item name="colorPrimaryDark">@color/darkPurple</item>
        <item name="colorAccent">@color/darkBlue</item>
        <item name="android:itemBackground">@color/darkBlue</item>
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationViewDark</item>
    </style>
    <style name="Widget.App.BottomNavigationViewDark" parent="Widget.Material3.BottomNavigationView">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.BottomNavigationView</item>
    </style>
</resources>